language: node_js
node_js:
  - 8
  
cache:
  yarn: true
  directories:
    - node_modules

before_install:
  - echo I am before installed

install:
  - echo I am install
  - yarn

before_script:
  - echo I am before_script

after_success:
  - echo I am after_success

after_failure:
  - echo I am after_failure

before_deploy:
  - echo I am before_deploy

after_script:
  - echo I am after_script

script:
  - echo I am script
  - yarn build
  - yarn test

stages:
  - name: deploy
    if: branch = master and type = pull_request
  - name: build
    if: branch = branch1 and type != pull_request

jobs:
  include:
    - stage: deploy
      name: ECS deployment
      env:
        - ENV=dev
        - MALAL=qa
      script:
        - echo "Deploying to ECS"
        - echo $ENV
        - echo $MALAL
    - stage: build
      name: NPM Build
      env:
        - ENV=dev
        - MALAL=qa
      script:
        - echo "Building YARN"
        - echo $ENV
        - echo $MALAL
